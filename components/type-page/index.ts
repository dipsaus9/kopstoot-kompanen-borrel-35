/**
 * The per-type page view (BORREL-4.6) — the /typetjes/[slug] centerpiece: one
 * Kompaan type rendered as a full character page in its own sub-theme, with the
 * image slot, full description, defining traits, signature colours and the list
 * of members with that type.
 */

export { TypePage, type TypePageProps } from "./type-page";
