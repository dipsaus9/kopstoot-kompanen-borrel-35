---
id: BORREL-1.1
title: Scaffold Next.js App Router app with Tailwind + ESLint
status: Done
assignee: []
created_date: '2026-08-14 13:46'
updated_date: '2026-08-14 13:57'
labels:
  - story
dependencies: []
references:
  - package.json
  - tsconfig.json
  - next.config.ts
  - next-env.d.ts
  - eslint.config.mjs
  - postcss.config.mjs
  - .gitignore
  - app/
  - public/
  - bun.lock
parent_task_id: BORREL-1
type: chore
ordinal: 2000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Scaffold a Next.js App Router + TypeScript app with Tailwind and ESLint via create-next-app, wired for bun, without clobbering existing repo files.

Type: deliverable
Branch: BORREL-1.1/scaffold-next-app
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 create-next-app (App Router, TypeScript, Tailwind, ESLint) scaffolded at repo root
- [x] #2 Existing README.md, .claude/, backlog/ and .git are preserved, not overwritten by the scaffold
- [x] #3 package.json has scripts dev, build, lint and typecheck (tsc --noEmit)
- [x] #4 bun run lint and bun run typecheck pass green
- [x] #5 bun run build succeeds
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Run create-next-app into a temp dir (bun, App Router, TS, Tailwind, ESLint, import-alias @/*). 2. Copy generated files into repo root, skipping README.md and existing .claude/ and backlog/. 3. Add typecheck script (tsc --noEmit). 4. bun install; run lint, typecheck, build.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
create-next-app refuses a non-empty dir, so scaffold in a temp dir then merge. Do NOT overwrite the bootstrap README.md, .claude/ or backlog/. Verify: bun run lint && bun run typecheck && bun run build.

Scaffolded via create-next-app in a temp dir then merged (skipping README.md/.claude/backlog). Next 15.5.23, React 19, Tailwind v4, ESLint 9. Added typecheck=tsc --noEmit. bun.lock committed for reproducible installs (not in original References — recommend widening). Added /.worktrees to .gitignore. Verify green: lint 0, typecheck 0, build ok.

Widened References to include bun.lock (reviewer scope-violation fix): the lockfile is an inseparable output of the scaffold and must be committed for reproducible installs/CI.

Review gate: round 1 = block (bun.lock scope violation), fixed by widening References; round 2 = PASS, no scope violations. Advisory: next-env.d.ts gitignored/generated (regenerated by build; typecheck green). Reviewer: dipsaus-ai:story-reviewer.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Scaffolded a Next.js 15.5 App Router app (React 19, TypeScript strict, Tailwind v4, ESLint 9) via create-next-app + bun, merged into the repo without clobbering README/.claude/backlog. Added typecheck script and /.worktrees to .gitignore. All verify green: lint, typecheck, build.
<!-- SECTION:FINAL_SUMMARY:END -->
